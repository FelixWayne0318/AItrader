# GitHub Actions 工作流: 智能提交分析
#
# 触发时机:
# 1. 每次 push 到 main 分支
# 2. 每次 PR 创建/更新
# 3. 手动触发
#
# 功能:
# 1. smart_commit_analyzer.py - 自动演进的回归检测 (规则自动从 git 生成)
# 2. analyze_commits_ai.py - AI 深度语义分析 (可选，需要 DEEPSEEK_API_KEY)

name: Commit Analysis

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:
    inputs:
      commits_to_analyze:
        description: '分析提交数量'
        required: false
        default: '100'
        type: string

jobs:
  # =========================================================================
  # Job 1: 智能回归检测 (自动演进，规则从 git 历史自动生成)
  # =========================================================================
  smart-analysis:
    name: Smart Regression Detection
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0  # 获取完整历史

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Run smart_commit_analyzer.py
        id: analyze
        run: |
          python3 scripts/smart_commit_analyzer.py \
            --commits ${{ github.event.inputs.commits_to_analyze || '100' }} \
            --json > smart_analysis.json 2>&1 || true

          # 提取结果
          TOTAL=$(python3 -c "import json; d=json.load(open('smart_analysis.json')); print(d.get('update', {}).get('total_rules', 0))")
          PASSED=$(python3 -c "import json; d=json.load(open('smart_analysis.json')); print(d.get('validation', {}).get('passed', 0))")
          FAILED=$(python3 -c "import json; d=json.load(open('smart_analysis.json')); print(d.get('validation', {}).get('failed', 0))")
          WARNINGS=$(python3 -c "import json; d=json.load(open('smart_analysis.json')); print(d.get('validation', {}).get('warnings', 0))")

          echo "total=$TOTAL" >> $GITHUB_OUTPUT
          echo "passed=$PASSED" >> $GITHUB_OUTPUT
          echo "failed=$FAILED" >> $GITHUB_OUTPUT
          echo "warnings=$WARNINGS" >> $GITHUB_OUTPUT

          # 显示结果摘要
          echo "📊 规则总数: $TOTAL"
          echo "✅ 通过: $PASSED"
          echo "❌ 失败: $FAILED"
          echo "⚠️ 警告: $WARNINGS"

      - name: Generate summary
        run: |
          echo "## 🔍 Smart Regression Detection" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "| Metric | Count |" >> $GITHUB_STEP_SUMMARY
          echo "|--------|-------|" >> $GITHUB_STEP_SUMMARY
          echo "| 📋 Total Rules | ${{ steps.analyze.outputs.total }} |" >> $GITHUB_STEP_SUMMARY
          echo "| ✅ Passed | ${{ steps.analyze.outputs.passed }} |" >> $GITHUB_STEP_SUMMARY
          echo "| ❌ Failed | ${{ steps.analyze.outputs.failed }} |" >> $GITHUB_STEP_SUMMARY
          echo "| ⚠️ Warnings | ${{ steps.analyze.outputs.warnings }} |" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY

          # 显示失败详情
          python3 -c "
          import json
          try:
              data = json.load(open('smart_analysis.json'))
              failed = data.get('failed_details', [])
              if failed:
                  print('### ❌ Failed Checks')
                  print()
                  for f in failed[:10]:
                      print(f\"- **{f.get('file', 'unknown')}** (commit {f.get('commit', '?')})\")
                      print(f\"  - {f.get('message', 'No details')}\")
          except Exception as e:
              print(f'Error: {e}')
          " >> $GITHUB_STEP_SUMMARY

      - name: Upload analysis results
        uses: actions/upload-artifact@v4
        with:
          name: smart-analysis-results
          path: |
            smart_analysis.json
            configs/auto_generated_rules.json

      - name: Comment on PR
        if: github.event_name == 'pull_request'
        uses: actions/github-script@v7
        with:
          script: |
            const total = '${{ steps.analyze.outputs.total }}';
            const passed = '${{ steps.analyze.outputs.passed }}';
            const failed = '${{ steps.analyze.outputs.failed }}';
            const warnings = '${{ steps.analyze.outputs.warnings }}';

            let status = failed > 0 ? '⚠️' : '✅';
            let body = `## ${status} Smart Regression Detection\n\n`;
            body += `**Auto-generated rules from git history**\n\n`;
            body += `| Metric | Count |\n|--------|-------|\n`;
            body += `| 📋 Total Rules | ${total} |\n`;
            body += `| ✅ Passed | ${passed} |\n`;
            body += `| ❌ Failed | ${failed} |\n`;
            body += `| ⚠️ Warnings | ${warnings} |\n`;

            if (failed > 0) {
              body += `\n⚠️ **Potential Regressions Detected**: Please review the failed checks in the workflow summary.`;
            }

            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: body
            });

  # =========================================================================
  # Job 2: AI 深度分析 (可选，需要 DEEPSEEK_API_KEY)
  # =========================================================================
  ai-analysis:
    name: AI Deep Analysis
    runs-on: ubuntu-latest

    steps:
      - name: Check API Key
        id: check_key
        env:
          DEEPSEEK_API_KEY: ${{ secrets.DEEPSEEK_API_KEY }}
        run: |
          if [ -z "$DEEPSEEK_API_KEY" ]; then
            echo "⚠️ DEEPSEEK_API_KEY not configured"
            echo "skip=true" >> $GITHUB_OUTPUT
          else
            echo "✅ DEEPSEEK_API_KEY available"
            echo "skip=false" >> $GITHUB_OUTPUT
          fi

      - name: Checkout code
        if: steps.check_key.outputs.skip != 'true'
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Python
        if: steps.check_key.outputs.skip != 'true'
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        if: steps.check_key.outputs.skip != 'true'
        run: pip install openai

      - name: Run AI analysis
        if: steps.check_key.outputs.skip != 'true'
        env:
          DEEPSEEK_API_KEY: ${{ secrets.DEEPSEEK_API_KEY }}
        run: |
          python3 scripts/analyze_commits_ai.py --commits 10 --json > ai_analysis.json 2>&1 || true

      - name: Generate AI summary
        if: steps.check_key.outputs.skip != 'true'
        run: |
          echo "## 🤖 AI Deep Analysis" >> $GITHUB_STEP_SUMMARY
          python3 -c "
          import json
          try:
              data = json.load(open('ai_analysis.json'))
              commits = data.get('commits', [])
              print(f'Analyzed {len(commits)} commits with DeepSeek AI')
              print()
              for c in commits[:5]:
                  print(f\"- **{c.get('hash', '')[:7]}**: {c.get('summary', 'No summary')[:80]}\")
          except Exception as e:
              print(f'Error: {e}')
          " >> $GITHUB_STEP_SUMMARY

      - name: Upload AI results
        if: steps.check_key.outputs.skip != 'true'
        uses: actions/upload-artifact@v4
        with:
          name: ai-analysis-results
          path: ai_analysis.json

      - name: Create skip marker
        if: steps.check_key.outputs.skip == 'true'
        run: |
          echo "## 🤖 AI Deep Analysis" >> $GITHUB_STEP_SUMMARY
          echo "⏭️ Skipped (DEEPSEEK_API_KEY not configured)" >> $GITHUB_STEP_SUMMARY
